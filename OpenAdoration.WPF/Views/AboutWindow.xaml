<Window x:Class="OpenAdoration.WPF.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About OpenAdoration"
        Width="440" Height="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource SurfaceBrush}">

    <StackPanel Margin="32,28,32,28">

        <!-- Identity -->
        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
            <Border Width="56" Height="56"
                    CornerRadius="28"
                    Background="{StaticResource AccentBrush}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,12">
                <TextBlock Text="✝"
                           Foreground="White"
                           FontSize="26"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Border>
            <TextBlock x:Name="AppNameText"
                       Text="OpenAdoration"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center" />
            <TextBlock x:Name="VersionText"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,3,0,0" />
            <TextBlock Text="Free open-source worship presentation software"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,6,0,0" />
        </StackPanel>

        <!-- Keyboard shortcuts -->
        <Border Background="{StaticResource PrimaryBrush}"
                CornerRadius="6"
                Padding="16,12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="KEYBOARD SHORTCUTS"
                           Foreground="{StaticResource AccentBrush}"
                           FontSize="10" FontWeight="SemiBold"
                           Margin="0,0,0,8" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Space / → / PgDn"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Next slide"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="← / PgUp"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="Previous slide"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="B"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="Blank screen"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Esc"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="Stop projection"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="1 – 9"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="Jump to slide N"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ctrl + 1–5"
                           Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,2" />
                <TextBlock Grid.Row="6" Grid.Column="1" Text="Navigate tabs"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2" />
            </Grid>
        </Border>

        <!-- License -->
        <TextBlock Text="Released under the MIT License · github.com/g0elles/openadoration"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="11"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap" />

    </StackPanel>
</Window>
